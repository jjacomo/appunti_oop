package a06.e2;

public interface Logic {
    // deve avere un metodo per controllare che hai cliccato un quadrato valido
    // boolean selection(int x, int y);
    boolean selection(int position);

    // Position advance();
}
